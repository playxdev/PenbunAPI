$kind: http-request
description: 200 · แก้ได้เฉพาะตอน DRAFT เท่านั้น มิฉะนั้น 422 BUSINESS_RULE. doc_no แก้ภายหลังไม่ได้ (NoUpdate)
url: "{{api}}/return-note/{{return_note_id}}"
method: PUT
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "remark": "รับคืนงวดสิงหาคม (แก้ไข)"
    }
order: 4000
