$kind: http-request
description: 201 · สร้าง header + items ในทรานแซกชันเดียว แล้วคำนวณยอดรวมใหม่จากรายการจริง. ห้ามส่ง doc_status / total_* / net_amount — ระบบเป็นผู้กำหนด
url: "{{api}}/vendor-return-note"
method: POST
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "doc_no": "VRN-2608-001",
      "doc_date": "2026-08-23",
      "remark": "ส่งคืนงวดสิงหาคม",
      "vendor_id": "{{vendor_id}}",
      "warehouse_id": "{{warehouse_id}}",
      "company_id": "{{company_id}}",
      "items": [
        {
          "sku_id": "{{sku_id}}",
          "qty_returned": 5,
          "unit_cost": 60,
          "remark": "คืนตามรอบ"
        }
      ]
    }
order: 3000
