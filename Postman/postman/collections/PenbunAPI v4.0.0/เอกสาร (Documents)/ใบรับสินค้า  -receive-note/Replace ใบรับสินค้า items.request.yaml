$kind: http-request
description: 200 · แทนที่รายการทั้งใบ (soft delete ของเดิม + insert ใหม่ + คำนวณยอดรวมใหม่). เฉพาะ DRAFT
url: "{{api}}/receive-note/{{receive_note_id}}/items"
method: PUT
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "items": [
        {
          "sku_id": "{{sku_id}}",
          "qty": 120,
          "unit_cost": 60,
          "cover_price": 100
        }
      ]
    }
order: 5000
