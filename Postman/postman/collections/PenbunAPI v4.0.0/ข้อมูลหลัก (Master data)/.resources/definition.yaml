$kind: collection
description: 21 resource สร้างจาก descriptor ตัวเดียวกัน — engine กลางให้ 5 endpoint ต่อ resource (book กับ user อ่านอย่างเดียว จึงมีแค่ 2 และ user ยังจำกัดไว้ที่ ADMIN)
order: 2000
