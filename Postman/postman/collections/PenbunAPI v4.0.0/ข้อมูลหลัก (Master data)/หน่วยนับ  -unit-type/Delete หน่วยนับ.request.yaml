$kind: http-request
description: 204 · soft delete (is_delete=1); trigger ตั้ง is_active=0 และ id_status='DELETED' ให้เอง. แถวที่ยังมีลูกอ้างอยู่ตอบ 409 REF_IN_USE
url: "{{api}}/unit-type/{{unit_type_id}}"
method: DELETE
order: 5000
