$kind: http-request
description: 200 · partial update — ส่งเฉพาะคอลัมน์ที่ต้องการแก้
url: "{{api}}/vendor-type/{{vendor_type_id}}"
method: PUT
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "description": "สำนักพิมพ์ในประเทศ"
    }
order: 4000
