$kind: http-request
description: 200 · partial update — ส่งเฉพาะคอลัมน์ที่ต้องการแก้
url: "{{api}}/book-type/{{book_type_id}}"
method: PUT
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "description": "รายเดือน"
    }
order: 4000
