$kind: http-request
description: 200 · partial update — ส่งเฉพาะคอลัมน์ที่ต้องการแก้
url: "{{api}}/customer-route/{{customer_route_id}}"
method: PUT
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "delivery_seq": 2
    }
order: 4000
