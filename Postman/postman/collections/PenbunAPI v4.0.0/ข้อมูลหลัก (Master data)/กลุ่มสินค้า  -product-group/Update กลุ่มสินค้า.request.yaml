$kind: http-request
description: 200 · partial update — ส่งเฉพาะคอลัมน์ที่ต้องการแก้
url: "{{api}}/product-group/{{product_group_id}}"
method: PUT
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "description": "มังงะแปลไทย (ลิขสิทธิ์)"
    }
order: 4000
