$kind: http-request
description: เข้าได้แม้ต้องเปลี่ยนรหัสผ่านก่อน — ไม่งั้นผู้ใช้จะติดวงวน
url: "{{api}}/auth/change-password"
method: POST
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "current_password": "{{password}}",
      "new_password": "NewPassw0rd!2026"
    }
order: 4000
