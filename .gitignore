# ความลับ — ห้ามขึ้น repository
.env
*.pem
*.key

# ผลลัพธ์การ build
bin/
*.exe

# เครื่องมือ
.idea/
.vscode/
*.log
coverage.out

# Postman CLI สร้าง workspace ซ้ำไว้ที่ราก ชี้ไป workspace เดียวกับ Postman/ ที่ track อยู่
# ปล่อยไว้จะสับสนว่า collection จริงอยู่ที่ไหน
penbun-api/
